# V0.8.0 Staging and Production Master Release Amendment

Status: ACCEPTED - ACTIVE
Owner: Earl
Accepted: 2026-08-09, Asia/Manila
Release: v0.8.0 Inventory Truth and Ledger Lock
Slice: 3 of 3 - final release continuation; no Slice 4
Branch: `release/v0.8.0-inventory-truth-ledger-lock`
Base specification: `.codex/specs/active/v0.8.0-inventory-truth-ledger-lock-slice-3.md`
Starting committed SHA: `c5f53ddf44aaf28ab4a3e43b74d42f66d09e257d`
Expected migration: none; schema 30 / latest `0030_production_access_and_operations.sql`

## Amendment and authority

Earl directly submitted the one-time master release prompt. It preserves the complete
accepted Slice 3 scope and explicitly amends the earlier stop state as follows:

- the repeatable Inventory classification browser failure may receive the smallest
  evidence-proven Slice 3 release-gate repair and regression;
- after a clean audit and local gates, one final Slice 3 candidate commit may be
  created, pushed, and placed in one protected PR with exact-head CI before remote
  staging access, resolving the previous sequencing conflict;
- after exact-head CI, private staging recovery proof and exact-SHA isolated-staging
  deployment/full-stack acceptance are authorized;
- when every mandatory audit, CI, review, staging, isolation, recovery, production
  read-only preflight, and rollback gate is green with no drift or unresolved P0/P1,
  production promotion is pre-authorized without another manual GO;
- protected merge, exact accepted-main production deployment, safe production smoke
  and reconciliation, v0.8.0 tag/GitHub Release, and repository closeout are authorized
  only through the current release runbook and exact accepted identity;
- any failed mandatory gate automatically revokes production authority for this run.

This remains Slice 3 of 3. If staging finds a code/runtime defect after the candidate
commit, stop rather than add another implementation commit. Documentation-only
closeout after verified external actions follows the repository release convention and
must not hide a runtime change.

### Test-only CI continuation authorization — 2026-08-09

After candidate `c6fe2bfa5d847f43f6abf0ef98ec40004920c8f0` passed exact-head CI but
PR browser-smoke proved `tests/e2e/role-experiences.spec.js` still asserted the old
v0.7.2 runtime label, Earl explicitly authorized one test-only follow-up commit and
continuation through staging and production. This amendment permits only:

- changing that stale test expectation to the already-frozen v0.8.0 runtime identity;
- updating repository continuity/audit records for this exact continuation;
- freezing and pushing one replacement candidate SHA and rerunning all invalidated
  local, exact-head, PR, staging, recovery, merge, and production gates.

It does not authorize any additional runtime/source/generated behavior change,
migration, history rewrite, force push, or new functional scope. Any non-test runtime
change or another red mandatory gate revokes production authority and stops the run.

## Required sequence

1. Resume and preserve the recorded dirty Slice 3 worktree; verify Git, release
   lineage, no competing writer, and no unknown work.
2. Audit the complete v0.8.0 diff, invariants, Slice 1 and Slice 3 repairs,
   reconciliation, security/privacy, migration identity, generated artifacts, and
   exact release tooling. Produce `MASTER_AUDIT: CLEAN` or stop.
3. Reproduce and classify the Inventory classification browser failure as
   `PRODUCT_DEFECT`, `TEST_HARNESS_DEFECT`, `ENVIRONMENTAL_FLAKE`, or `UNKNOWN_STOP`;
   perform at most two bounded repair attempts, then rerun all invalidated gates.
4. Require focused Inventory/direct-caller/Worker-D1/browser checks, canonical
   `npm run check`, governance, handoff, diff, secret/PII/private-identifier scan,
   complete logical review, and zero unresolved P0/P1.
5. Freeze one exact v0.8.0 candidate commit at schema 30/0030, push the existing
   branch, create/reuse one protected PR, and obtain exact-head CI/review.
6. Using owner-private configuration outside Git/logs, prove exact staging
   Worker/D1/R2 identity and production isolation; capture Worker/config/R2 rollback
   fingerprints and fresh D1 export/recovery point; restore in isolation and require
   integrity `ok`, zero FK violations, schema 30/0030, immutable history, and
   reconciliation.
7. Deploy only the exact candidate to isolated staging and require runtime 0.8.0,
   exact SHA, readiness/protection, staging identity, safe public/authenticated module
   smoke, D1/R2 isolation, and changed-path acceptance. Use exact-code proof and
   `LIVE_MUTATION_NOT_SAFE` when sandbox-safe mutation is unavailable.
8. Before production writes, perform exact private read-only production audit and
   Inventory reconciliation, then capture a fresh production D1 export, Time Travel or
   accepted recovery coordinate, isolated restore, Worker rollback version, R2/config
   fingerprints, and require `PRODUCTION_RECOVERY: READY`.
9. Merge through protected GitHub only with exact candidate head/check/review parity;
   record the exact accepted main SHA and deploy only its approved artifact.
10. Require production runtime 0.8.0, exact release SHA, schema 30/0030,
    readiness/protection, no staging metadata, bounded public/auth/internal/D1/R2
    smoke, and post-deploy reconciliation with zero unexpected business mutation.
11. Publish tag and GitHub Release v0.8.0 only at the exact accepted/deployed identity,
    close repository continuity, release the writer lock, and set the next task to the
    separately accepted Isolated Staging Playground conversion before v0.8.1.

## Non-negotiable boundaries

- No migration 0031 or other schema change.
- No force push, unknown-work reset/clean, immutable ledger/audit/status/history
  rewrite, generic D1 deletion, production auth bypass, staging/production binding
  crossover, Google Inventory authority, secret/private value logging, or non-exact
  deployment.
- No production action unless every mandatory condition is green; stop on target,
  SHA, schema, binding, privacy, recovery, reconciliation, or authorization drift.
- No real provider/email or Google write unless the current runbook explicitly
  requires and separately proves it safe; this release expects none.
- No automatic v0.8.1 work or playground implementation without its separate accepted
  specification.

## Acceptance

Success is `RELEASED` only when the exact candidate passes local/CI/review/staging and
recovery gates, the protected merge is exact, production deploy/smoke/reconciliation
are green, release identity is finalized, production rollback remains ready, and the
writer lock is released. Otherwise preserve work/evidence and report `STOPPED` or
`ROLLED_BACK` with the exact blocker.
