# V0.8.0 Inventory Truth and Ledger Lock — Slice 2

Status: ACCEPTED — COMPLETE
Owner: Earl
Accepted: 2026-08-09, Asia/Manila
Slice: 2 of at most 3 — Ledger, Reservation, Concurrency, and Retry-Safety Hardening
Starting SHA: `77286cc65827070c7d93a07eaf4454c28d2d1147`
Branch: `release/v0.8.0-inventory-truth-ledger-lock`
Production boundary: unchanged v0.7.2 at `84eacfcdb47a3985fed48e3ba14bb413946d4410`, schema 30/0030
Isolated staging boundary: unchanged v0.7.2 at `c4fa46f267733eeceb5d82a825431c6337f8e4e0`, schema 30/0030

## Owner acceptance and bounded authority

Earl directly submitted the Slice 2 execution prompt. Gate 0 proved the accepted,
clean, pushed Slice 1 checkpoint at the starting SHA above, with no runtime or
migration change, no active writer, no unresolved P0/P1, and
`MIGRATION_DECISION: NONE_REQUIRED`.

This specification authorizes only bounded repository implementation on the existing
release branch, focused local/Worker-D1 verification, one clean Slice 2 commit, and a
preservation push of that branch. It does not authorize a new branch, schema or
migration change, staging deployment/reset/seed, provider call, Google write, remote
D1/R2 mutation, production mutation/deployment, version bump, tag, release, PR or
merge, branch cleanup, reconciliation/cutover, or Slice 3.

## Objective and evidence boundary

Repair only the four Slice 1 gaps recorded in
`docs/INVENTORY_TRUTH_BASELINE.md`. Every runtime behavior change must map to a
`REPAIR_IN_SLICE_2` entry in `docs/INVENTORY_SLICE_2_REPAIR_REGISTER.md`.
Do not invent hardening work or redesign Inventory.

The desired result preserves D1 as the one structured operational authority, R2 as
the governed evidence/file authority where applicable, and Google as secondary
projection/import/reconciliation/archive only. Accepted movements remain explicit,
append-only, atomic, server-authorized, privacy-safe, concurrency-safe, and retry-safe.

## Accepted repair set

- `V080-S1-INV-01`: repair the claimed event-item transfer client/server mismatch
  with the existing D1 atomic-batch, authorization, idempotency, ledger, audit/history,
  and revision architecture. Preserve one logical transfer identity and paired
  transfer-out/transfer-in effects. Do not create a new transfer product family.
- `V080-S1-INV-02`: add the smallest governed active-reservation cancellation/release
  path integrated with current accepted Request/Lending semantics. Preserve on-hand,
  restore ATP, append history/audit, authorize server-side, and make retries one-effect.
- `V080-S1-INV-03`: prevent distinct stale cycle-count commands from double-applying
  one physical count. Reuse the current revision/sentinel transaction pattern under
  schema 30 and preserve append-only adjustment/audit/idempotency effects.
- `V080-S1-INV-04`: correct only the proven no-server presentation sign reducers by
  using explicit signed quantity or one transaction-type-aware helper. Authoritative
  D1-supplied production balances and unrelated UI remain unchanged.

If reproduction shows an item is already closed without runtime change, reclassify it
`TEST_ONLY_ALREADY_CLOSED`. If the root cause expands materially, requires schema 31,
historical reconciliation, immutable-history rewrite, ambiguous authorization/privacy,
or private/remote data, stop that item and record the smallest required amendment.

## Required bug-fix method

For every accepted repair: reproduce on the verified starting SHA; confirm the failure
matches Slice 1; add the smallest regression before the fix when practical; change only
the authoritative boundary; run the exact regression, adjacent invariant tests, direct
caller tests, and relevant Worker-D1 tests; review the logical diff; and record the
result against the defect ID. Do not leave the branch intentionally red.

Use deterministic transaction ordering, controlled hooks, or D1 guards rather than
sleep-based races. Where applicable prove paired transfer, reservation release without
on-hand change, stale-state rejection, one-effect duplicate retry, no partial commit,
and authorization denial without business effect.

## Invariants

Preserve INV-01 through INV-10 exactly as defined in the Slice 1 specification and
baseline: one physical truth; reservations and request submission do not deduct
on-hand; availability is consistent; stock effects use explicit history; retries have
one effect; authorization/privacy are server-enforced; history is append-only;
accepted paths cannot create negative/impossible state; and Google is not runtime
Inventory authority.

No current historical staging or production record may be guessed, normalized,
rewritten, deleted, or reconciled in this slice.

## Deliverables

- This accepted specification and an explicit Slice 2 Repair Register.
- Focused regression tests and minimal runtime/service/transaction repairs only for
  `REPAIR_IN_SLICE_2` entries.
- Post-repair INV-01 through INV-10 matrix with only `PASS`,
  `PASS_WITH_EXISTING_EVIDENCE`, `DEFER_SLICE_3_RECONCILIATION`, or
  `STOP_REQUIRES_AMENDMENT`.
- Updated current pointer/task/handoff, Inventory baseline, project status,
  continuation, append-only changelog, and architecture pointer only where needed.
- One clean Slice 2 commit and authorized preservation push of the existing branch.
- Released writer lock and next action limited to awaiting Earl's Slice 3 approval.

## Verification and acceptance

Before product edits run `npm run handoff:verify` and `npm run check:governance`.
For each repair run its exact regression plus affected service/domain/Worker-D1/direct
caller and adjacent invariant tests. After all repairs run the focused matrix for the
changed boundaries, relevant lint/static/build/parity checks, `git diff --check`, a
targeted secret/PII/private-identifier scan, and a full logical diff review from the
starting SHA. Run the full repository gate only if shared-core changes or focused
failures create cross-cutting uncertainty or repository governance requires it.

Completion requires every runtime change to map to Slice 1, no new truth source, atomic
and retry-safe changed stock effects, safe reservation/stale-state behavior, no
impossible accepted state, preserved cumulative/paired semantics, unchanged-or-stronger
authorization/privacy, passing affected direct callers, no protected external mutation,
and a clean pushed single Slice 2 commit with Slice 3 unstarted.

## Stop conditions

Stop for missing/contradictory Slice 1 evidence; migration decision other than
`NONE_REQUIRED`; another writer; unknown dirty or divergent work; schema/migration
need; immutable-history rewrite; historical-data guess/reconciliation need;
authorization/privacy ambiguity; production/staging crossover; materially expanded
root cause; new out-of-scope P0/P1; two failed bounded repair attempts; inability to
prove correctness without unauthorized private/production data; or secret/private
identifier exposure.

The repository rollback boundary is the starting SHA. No remote database backup is
required because no remote data mutation is authorized. Slice 2 must stop after its
handoff and must not begin Slice 3 automatically.
