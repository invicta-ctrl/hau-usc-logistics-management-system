# v0.8.3 Identity Intake — A5 Accepted Minimal Scope

STATUS: ID_A_COMPLETE_ID_B_BLOCKED_ON_OWNER_DECISIONS
OWNER_AUTHORITY: Earl A5 plus the current explicit continuation instruction
BASE_BRANCH: main
BASE_SHA: 7d826f2683fbca8058ff08e8ae40acc1e095c076
BRANCH: release/v0.8.3-identity-foundation
COMPANION: Private v0.8.2-v0.8.5 Codex Preflight Companion, V083 map

## Purpose

Freeze the post-v0.8.2 identity baseline and prove that the existing
authentication/session contract remains preserved before any person or
assignment expansion is considered.

## Accepted slice: ID-A only

- Verify existing token-digest, CSRF, credential-version, expiry, rate-limit,
  generic-error, and session-revocation behavior with the current focused
  contract tests.
- Record a privacy-safe, no-migration baseline against schema 30 and migration
  `0030_production_access_and_operations.sql`.
- Preserve account, account-committee, account-access-policy, account
  application, protected roster, and audit/history contracts unchanged.

## Explicitly excluded

- Person, contact, assignment, or account-person schema/repository changes.
- Migrations, backfill/import, roster or Google reads, Sheets/Drive writes,
  provider actions, credential changes, deployment, Playground, Production,
  recovery-pointer work, and generated artifacts.
- Frontend adoption, Make inspection, and route-design work until both required
  acknowledgements and `READY_FOR_ADOPTION` are recorded.

## ID-B decision gate

This intake does **not** infer or approve the person/assignment model. Before
ID-B or any schema/mapping/linkage work, Earl must accept a decision table for:

1. stable person key;
2. email cardinality and mutability;
3. one-person/multiple-account policy;
4. duplicate-assignment representation;
5. assignment effective dating, source authority, and rollback;
6. authorization-projection timing and owner-review behavior.

Until that table is accepted, `ID-B` through `ID-H` are blocked. The only
permitted implementation outcome for this packet is an evidence-backed ID-A
preservation no-op.

## Acceptance criteria

- Current branch and remote lineage match this packet's base before the intake
  commit and remain clean except for its declared documents.
- Focused auth/session/repository contracts pass without raw roster data,
  credentials, private identifiers, or provider output entering Git or logs.
- No migration or external mutation occurs.
- The current-chain records and this packet truthfully state the ID-B gate and
  the next exact action.

## Verification

The focused auth/session contract set passed: 7 files and 42 tests under the
established Node 22 toolchain. Markdown formatting, `git diff --check`,
`handoff:verify`, and `check:governance` remain the only required closeout
checks. Do not run a broad release matrix for this intake.
