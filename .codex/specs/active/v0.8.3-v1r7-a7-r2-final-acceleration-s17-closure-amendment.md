# HAU-USC Logistics — V1R7-A7-R2 v0.8.3 Final Acceleration and S17 Closure Amendment

**INTENT:** SOFTWARE_FEATURE / TESTING / MIGRATION / DEPLOYMENT / RELEASE_CLOSEOUT<br>
**SECONDARY INTENTS:** OWNER_DECISION, GOVERNANCE_AMENDMENT, CONTEXT_EFFICIENCY, REPOSITORY_MAINTENANCE<br>
**MODE:** ADOPT ACTIVE WORK → FINISH ID-G → FINISH ID-H PROVIDER-FREE → FREEZE EXACT CANDIDATE → PLAYGROUND + EXTERNAL GATES → EARL GO → PRODUCTION → S17 → STOP<br>
**OWNER:** Earl<br>
**DATE:** 2026-08-20<br>
**TIMEZONE:** Asia/Manila<br>
**TARGET:** `invicta-ctrl/hau-usc-logistics-management-system`<br>
**TARGET RELEASE:** `v0.8.3 — PIH Identity / Directory Foundation`<br>
**RISK:** HIGH<br>
**AMENDMENT ID:** `V1R7-A7`<br>
**REVISION:** `R2`<br>
**TITLE:** `v0.8.3 Final Acceleration and S17 Closure Amendment — Final Audited Revision`

---

# 0. OWNER AUTHORIZATION

By submitting this exact R2 amendment to the active HAU-USC Logistics Codex session, Earl accepts it as the controlling **v0.8.3 execution amendment**.

The objective is:

> **Finish v0.8.3 now, safely, without further unnecessary plan/audit loops, while preserving exact-candidate identity, migration safety, private-data boundaries, isolated Playground acceptance, explicit Production GO, rollback, reconciliation, and S17 closure.**

This amendment does **not** reduce the accepted v0.8.3 product scope.

It changes only the execution of that accepted scope by:

- making the previously ambiguous ID-B–ID-H bookkeeping explicit;
- removing the false `ID_D=0` bookkeeping blocker;
- adopting the already-running Activity History implementation instead of restarting it;
- combining the verification-lifecycle requirements into one coherent ID-H slice;
- using proportionate deterministic verification and evidence reuse;
- limiting repeated plan/review loops;
- correcting candidate-freeze versus live-provider gate sequencing;
- resolving the 0031/0032 release decisions;
- authorizing the exact bounded Playground/provider checks needed before Production;
- preserving one mandatory owner pause for manual Playground acceptance and Production GO;
- stopping after v0.8.3 S17 rather than automatically beginning v0.8.4.

This amendment does **not** waive:

- the accepted v0.8.3 specification;
- one canonical writer;
- no-fabrication rules;
- authentication/authorization safety;
- private-data and secret boundaries;
- migration verification;
- isolated Playground resources;
- exact candidate SHA/tree/artifact identity;
- backup and rollback;
- Earl's manual Playground test;
- Earl's explicit Production GO;
- Production smoke and reconciliation;
- recovery-pointer rotation;
- S17 durable closure.

No v0.8.4 implementation may begin in this Codex session.

---

# 1. FINAL AUDIT BASIS

This R2 revision was audited against:

## 1.1 Current GitHub Context Vault authority

Fresh Git fetch from:

```text
https://github.com/invicta-ctrl/gpt-context-vault
```

verified:

```text
REMOTE main:
fe69dc2191deac683d09eebaee56ff922f870e7a

LOCAL canonical Context Vault main:
fe69dc2191deac683d09eebaee56ff922f870e7a

DIVERGENCE:
0 / 0
```

The following canonical Context Vault authorities were used:

```text
AGENTS.md
START_HERE.md
CONTEXT_INDEX.md
protocols/CODEX_TOKEN_OPTIMIZATION_AND_CONTEXT_EFFICIENCY_RULES.md
governance/agents/specs/TOKEN-OPT-001-A1.md
protocols/AI_ASSISTED_SDD_PROTOCOL.md
protocols/INCREMENTAL_CODEX_CONTEXT_PROTOCOL.md
governance/agents/AGENTS_REGISTRY.json
governance/agents/extensions/hau-usc-logistics.PROJECT_POLICY.md
```

## 1.2 Important Context Vault registry state

The HAU-USC registered project extension exists in the Context Vault source, but its target synchronization is currently gated:

```text
HAU extension target:
D:\Documents\Codex\HAU-USC Logistics\active\hau-usc-logistics-management-system\.agents\PROJECT_POLICY.md

CURRENT REGISTRY GATE:
BLOCKED_ACTIVE_WRITER_AND_DIRTY_WORK

sync_allowed:
false
```

Therefore:

> **Do not treat an unsynchronized `.agents/PROJECT_POLICY.md` target as already-active local repository authority.**

For the current v0.8.3 release, use the actual checked-in HAU repository `AGENTS.md` plus the accepted specification/current chain.

This R2 amendment explicitly adopts the relevant stricter Context Vault efficiency rules as **owner-approved v0.8.3 task constraints**.

Do not synchronize AGENTS/project-policy files during the active v0.8.3 writer window.

## 1.3 Current HAU-USC repository state

Observed canonical release worktree:

```text
D:\Documents\Codex\HAU-USC Logistics\worktrees\v081-production-execution-eb14cd81
```

Observed state at R2 audit:

```text
BRANCH:
release/v0.8.3-identity-foundation

HEAD:
f3c28ba257e80fabb532979969ddd27cab0959db

UPSTREAM:
origin/release/v0.8.3-identity-foundation

AHEAD / BEHIND:
0 / 0

CANONICAL WORKTREE:
CLEAN

ACTIVE_WRITER:
TERRA_MAX:/root/v83_completion_terra_writer

WRITER_LOCK:
HELD

CURRENT MILESTONE:
V83_STAFF_ACCOUNT_ACTIVITY_HISTORY_PROVIDER_FREE_IMPLEMENTATION
```

Observed active implementation worktree:

```text
D:\Documents\Codex\HAU-USC Logistics\worktrees\v083-staff-account-activity-history-f3c28ba
```

At R2 audit it contained **26 changed/untracked paths**, and deterministic comparison against `CURRENT_TASK.md` proved:

```text
OUTSIDE IMPLEMENTATION_OWNED_PATHS:
0
```

The implementation is active and in scope.

**Preserve it. Do not restart it.**

---

# 2. R2 CORRECTIONS TO THE FIRST A7 DRAFT

R2 supersedes the first A7 draft.

The first draft had two release-sequencing problems that R2 corrects.

## 2.1 Correction 1 — live source probe cannot be a pre-freeze requirement

The existing source-projection probe is candidate-bound and requires the exact candidate SHA plus the isolated Playground runtime.

Therefore this sequence is invalid:

```text
live probe PASS
-> freeze candidate
```

because the probe itself requires a frozen candidate identity.

R2 uses:

```text
provider-free product scope green
-> freeze exact candidate
-> deploy exact candidate to isolated Playground
-> run candidate-bound read-only source reconciliation
-> block Production promotion on mismatch
```

## 2.2 Correction 2 — live email delivery cannot be required before candidate freeze

The ID-H code/security contract can be proven provider-free before freeze.

Actual provider delivery proof belongs to Playground acceptance.

Therefore R2 distinguishes:

```text
ID-H_IMPLEMENTATION = PASS
```

from:

```text
ID-H_PLAYGROUND_DELIVERY_GATE = PASS
```

The first is required to freeze.

The second is required before Earl's Production GO.

## 2.3 Candidate freeze versus Production eligibility

A frozen candidate can exist while an external Playground gate is still pending.

If the live source or email provider gate fails:

```text
CANDIDATE remains frozen
PRODUCTION_ELIGIBLE = FALSE
```

unless source/config/code changes require a new candidate.

A failed external check is not permission to mutate Production or silently alter private data.

---

# 3. AUTHORITY ORDER

Use this exact order:

1. Earl's current explicit instruction and this accepted `V1R7-A7-R2` amendment.
2. Accepted v0.8.3 specification:
   - `.codex/specs/active/v0.8.3-identity-intake-a5-accepted.md`
3. Accepted master program:
   - `.codex/specs/active/v0.8.1-v0.8.5-v1r7a3-standalone-master-program.md`
4. Actual checked-in HAU repository `AGENTS.md`.
5. `.codex/CURRENT.md`
6. `.codex/CURRENT_TASK.md`
7. `.codex/CURRENT_HANDOFF.md`
8. `.codex/PHASE_AND_CONTEXT_POLICY.md`
9. `.codex/releases/v0.8.3/V0_8_3_EXECUTION_PACKET.md`
10. `.codex/releases/v0.8.3/V0_8_3_SCOPE_COMPLETENESS_MATRIX.md`
11. Direct current source, tests, migration, CI, Playground, Production, provider, and release evidence.
12. Freshly fetched Context Vault universal governance for the specific efficiency/task constraints explicitly adopted by this amendment.

A material contradiction is a stop condition.

Do not silently combine incompatible instructions.

---

# 4. CONTEXT VAULT EXECUTION RULES ADOPTED FOR THIS V0.8.3 TASK

Use:

```text
MAXIMIZE VERIFIED PROGRESS PER TOKEN
GOVERNANCE WINS OVER TOKEN SAVINGS
STOP WHEN GREEN
```

For this v0.8.3 completion task, Earl explicitly imposes:

```text
ONE CANONICAL WRITER
ZERO CHILDREN BY DEFAULT
AT MOST ONE ACTIVE READ-ONLY CHILD WHEN JUSTIFIED
DELEGATION DEPTH = 1
DETERMINISTIC TOOLS FIRST
MINIMUM SUFFICIENT CONTEXT
REUSE FRESH VERIFIED EVIDENCE
RISK-TRIGGERED INDEPENDENT REVIEW
NO REPEATED TEST/BUILD/REVIEW WITHOUT AN INVALIDATOR
FIRST SAME-ROOT FAILURE -> DIAGNOSE
SECOND SAME-ROOT FAILURE -> STOP REPEATING THAT STRATEGY
```

These are **task-specific stricter limits for v0.8.3**.

They do not permanently rewrite repository-wide model limits in `AGENTS.md`.

---

# 5. REQUIRED REHYDRATION HANDSHAKE

Before adopting this amendment, reverify:

```text
REPOSITORY_ROOT:
CANONICAL_WORKTREE:
CANONICAL_BRANCH:
CANONICAL_HEAD:
CANONICAL_TREE:
UPSTREAM:
AHEAD_BEHIND:
CANONICAL_STATUS:

ACTIVE_WRITER:
WRITER_LOCK:
LOCK_HOLDER:

ACTIVE_IMPLEMENTATION_WORKTREE:
ACTIVE_IMPLEMENTATION_BRANCH:
ACTIVE_IMPLEMENTATION_HEAD:
ACTIVE_IMPLEMENTATION_STATUS:
UNCOMMITTED_PATH_COUNT:
OUTSIDE_AUTHORIZED_PATH_COUNT:

ACCEPTED_SPEC:
CURRENT_TASK:
CURRENT_MILESTONE:

PRODUCTION_VERSION:
PRODUCTION_SHA:
PRODUCTION_SCHEMA:
PRODUCTION_MIGRATION:

PLAYGROUND_CURRENT_VERSION:
PLAYGROUND_CURRENT_SHA:
PLAYGROUND_CURRENT_SCHEMA:

OPEN_P0:
OPEN_P1:
```

Expected known Activity History dirty work is not a blocker when:

- it is in the already-authorized isolated worktree;
- it belongs to the current Terra writer;
- every changed path remains inside `IMPLEMENTATION_OWNED_PATHS`.

Stop on:

- conflicting writer;
- unknown dirty work;
- path outside authorized scope;
- unexplained divergence;
- wrong branch;
- private-data exposure;
- provider crossover;
- material authority contradiction.

Never reset, clean, discard, overwrite, or recreate the current Activity History work.

---

# 6. EXPLICIT ID-B THROUGH ID-H MAPPING

The accepted v0.8.3 specification says:

```text
Complete ID-B through ID-H one bounded, reviewable slice at a time:
canonical person and assignment domain,
email/provenance and explicit account linkage,
existing active-access preservation,
two-stage approval,
Staff Directory,
activity history,
secure eight-digit verification with resend invalidation/backoff
and existing email delivery integration where required.
```

There are exactly seven listed capabilities for seven identifiers.

V1R7-A7-R2 explicitly accepts the ordered mapping:

```text
ID-A = auth/session/security preservation

ID-B = canonical person and assignment domain

ID-C = email/provenance and explicit account linkage

ID-D = existing active-access preservation

ID-E = two-stage approval

ID-F = Staff Directory

ID-G = staff/account operational activity history

ID-H = secure 8-digit verification lifecycle:
       8-digit format
       cryptographically secure generation
       leading-zero preservation
       expiry
       single use
       resend invalidation
       attempt controls
       rate-limit/backoff
       existing email-delivery integration
```

This is a **new explicit owner mapping**.

Do not claim older records had already assigned these letters individually.

---

# 7. ID-D BOOKKEEPING BLOCKER IS RESOLVED

Under the accepted R2 mapping:

```text
ID-D = existing active-access preservation
```

The current v0.8.3 scope matrix already records:

```text
Existing effective-access preservation = PASS
Authorization projection / no inferred privilege = PASS
```

Therefore, after revalidating that the relevant evidence remains unchanged:

```text
ID-D = PASS
```

The live Google/source-projection probe is not ID-D.

Reclassify it only in release documentation as:

```text
SOURCE_RECONCILIATION_PLAYGROUND_GATE
```

Do not rename the tested code/API merely to match the new documentation label.

Do not retain `ID_D=0` as a release blocker after this amendment unless a real regression in existing active-access preservation is proven.

---

# 8. V0.8.3 PRODUCT SCOPE AFTER R2

The product matrix becomes:

| ID | Capability | Current disposition to reverify |
|---|---|---|
| ID-A | Auth/session/security preservation | `VERIFIED_NO_OP` |
| ID-B | Canonical person + assignment domain | `PASS` |
| ID-C | Email/provenance + explicit account linkage | `PASS` |
| ID-D | Existing active-access preservation | `PASS` |
| ID-E | Two-stage approval | `VERIFIED_NO_OP` |
| ID-F | Staff Directory | `PASS` |
| ID-G | Activity History | `ACTIVE / REMAINING until verified canonical integration` |
| ID-H | Secure 8-digit verification lifecycle implementation | `REMAINING` |

Separate accepted requirement:

```text
PASSWORD_VISIBILITY = PASS
PASSWORD_BROWSER_EVIDENCE = PASS
```

Separate external/release gates:

```text
MIGRATION_0031_DECISION
MIGRATION_0032_DECISION
CANDIDATE_FREEZE
PLAYGROUND_MIGRATION_ACCEPTANCE
SOURCE_RECONCILIATION_PLAYGROUND_GATE
ID_H_PLAYGROUND_DELIVERY_GATE
PLAYGROUND_AUTOMATED_ACCEPTANCE
PLAYGROUND_BROWSER_ACCEPTANCE
EARL_MANUAL_TEST
EARL_PRODUCTION_GO
PRODUCTION_PREFLIGHT
PRODUCTION_RELEASE
PRODUCTION_SMOKE
PRODUCTION_RECONCILIATION
RECOVERY_POINTER_ROTATION
S17
```

Do not inflate the product matrix by treating expiry, resend, backoff, and email integration as separate releases. They are acceptance criteria inside ID-H.

---

# 9. ACCELERATION RULE — NO MORE UNNECESSARY PLAN LOOPS

Activity History has already passed the accepted plan-review gate.

Do not create another Activity History plan revision or plan-only Luna audit unless:

- implementation proves a material accepted-spec contradiction;
- a deterministic migration/D1 test proves the accepted design unsafe or impossible;
- a required security/privacy invariant cannot be satisfied;
- a new owner decision is genuinely required.

Historical rejected plan audits remain evidence only.

They are not instructions to restart the planning process.

---

# 10. REVIEW POLICY FOR THE REST OF V0.8.3

For ID-G and ID-H:

```text
IMPLEMENT
-> FOCUSED DETERMINISTIC VERIFICATION
-> COMPLETE LOGICAL DIFF REVIEW
-> ONE INDEPENDENT LUNA IMPLEMENTATION AUDIT WHEN RISK REQUIRES IT
-> REPAIR MATERIAL P0/P1/P2 FINDINGS
-> REVERIFY ONLY INVALIDATED EVIDENCE
-> INTEGRATE
-> STOP THAT SLICE
```

A second independent audit is allowed only when:

- the repair materially changes migration/security/authorization/privacy architecture;
- the previous review explicitly identifies a named high-risk finding requiring re-review;
- a new deterministic failure appears.

P3 advisory findings do not block completion unless they violate accepted scope.

Do not run review merely because reviewer capacity exists.

---

# 11. TEST AND EVIDENCE REUSE

Before rerunning an expensive check ask:

```text
DID RELEVANT SOURCE CHANGE?
DID RELEVANT CONFIGURATION CHANGE?
DID THE ARTIFACT CHANGE?
DID THE RELEVANT ENVIRONMENT OR EXTERNAL STATE CHANGE?
```

If every answer is `NO`, reuse the prior verified evidence.

When rerunning, record:

```text
REVERIFY_REASON:
<exact changed state and invalidated evidence>
```

Use:

```text
focused test
-> affected subsystem/family
-> one final release gate
```

Do not run the full release gate after each small correction.

---

# 12. ID-G — FINISH THE ACTIVE ACTIVITY HISTORY IMPLEMENTATION

Adopt the existing isolated implementation worktree exactly as it exists.

Do not create a replacement Activity History branch/worktree.

Do not create another Activity History writer.

The current Terra writer finishes the already-authorized Section 6 implementation.

## 12.1 Current implementation preservation

At R2 audit:

```text
ACTIVE WORKTREE:
D:\Documents\Codex\HAU-USC Logistics\worktrees\v083-staff-account-activity-history-f3c28ba

CHANGED / UNTRACKED PATHS:
26

OUTSIDE CURRENT_TASK IMPLEMENTATION_OWNED_PATHS:
0
```

This means the currently observed implementation is expected authorized work.

Continue it.

## 12.2 Required ID-G outcome

Complete the accepted Activity History plan including, where required by the accepted plan:

```text
migration 0032
strict append-only history
event-time identity attribution
persisted audit/transition operation context
no delayed current-state attribution
identity immutability guards
assignment-fingerprint immutability
safe DTO
ACCESS_ADMIN read-only route
query/filter/pagination
V5 Activity History rendering
provider-free privacy boundaries
local SQLite/D1/Miniflare migration and trigger proof
forward-fix / rollback disposition
```

## 12.3 Required ID-G verification

Use the already-accepted Activity History Section 7 matrix.

Minimum:

```text
focused migration tests
focused repository tests
focused account-application producer tests
focused access-management producer tests
focused service tests
Worker route tests
affected V5 tests
focused browser tests where V5 behavior changed
real local Miniflare/D1 migration + trigger execution
Node syntax
scoped ESLint
scoped Prettier
git diff --check
protected-field/privacy scan
complete logical diff review
```

Do not execute migration 0032 against Playground or Production in the implementation slice.

## 12.4 ID-G completion

When targeted evidence is green:

1. run one independent Luna implementation audit;
2. repair material P0/P1/P2 findings only;
3. reverify affected evidence;
4. commit and push the isolated implementation normally;
5. integrate through the currently accepted canonical integration mechanism;
6. prove canonical release branch parity;
7. mark ID-G `PASS`;
8. update the scope matrix once;
9. do not start another ID-G review loop.

---

# 13. OPTIONAL ID-H READ-ONLY SCOUT

While Terra finishes ID-G, at most one read-only Luna scout may prepare ID-H if the single child slot is otherwise free.

The scout is optional.

It may not:

- write;
- touch the active Activity History worktree;
- access private provider values;
- send email;
- create a migration;
- begin ID-H implementation;
- spawn another agent.

Required output:

```text
ID_H_SCOUT_STATUS:
SCOUT_BASELINE_SHA:
STALE_IF:

8_DIGIT_FORMAT:
CRYPTO_SECURE_GENERATION:
LEADING_ZERO_PRESERVATION:
EXPIRY:
SINGLE_USE:
RESEND_INVALIDATION:
ATTEMPT_LIMIT:
BACKOFF:
PLAINTEXT_LOGGING:
EMAIL_PROVIDER_CONTRACT:
RECIPIENT_ALLOWLIST_CONTRACT:
BROWSER_FLOW:

EXACT_EXISTING_TESTS:
MISSING_TESTS:
MINIMAL_REQUIRED_CHANGES:
LIKELY_OWNED_PATHS:
PROVIDER_LIVE_PROOF_REQUIRED:
SECURITY_PRIVACY_GATES:
DO_NOT_REPEAT:
NO_WRITE_ATTESTATION:
```

At ID-G completion, revalidate only the ID-G delta capable of invalidating this packet.

Because Activity History currently touches account-application paths, those shared paths must be rechecked before adopting the scout result.

---

# 14. ID-H — ONE COHERENT PROVIDER-FREE IMPLEMENTATION SLICE

After ID-G canonical integration, immediately execute ID-H.

No new owner decision is required unless a true safety/external-truth blocker appears.

## 14.1 Classify before modifying

For each criterion classify:

```text
PASS_EXISTING
REPAIR_REQUIRED
PLAYGROUND_LIVE_PROOF_REQUIRED
```

Do not rewrite an already-correct implementation.

## 14.2 Provider-free ID-H security contract

Before candidate freeze, deterministic code/test evidence must prove:

- exactly 8 decimal digits;
- leading zeroes preserved;
- cryptographically secure generation;
- single use;
- accepted expiry;
- resend invalidates the prior outstanding code;
- bounded verification attempts;
- rate-limit/backoff;
- plaintext verification code is never persisted to logs;
- provider failure cannot silently mark the email verified;
- verification proves email control only;
- Administrator review remains separate;
- Director approval remains separate;
- activation remains explicit;
- server-side authorization remains authoritative;
- provider configuration is fail-closed when missing/malformed;
- safe recipient/allowlist behavior is enforced where required.

## 14.3 Provider-free ID-H verification

Run only the affected set:

```text
account-application service/repository tests
verification lifecycle tests
email-provider contract tests
resend-provider tests
rate-limit/backoff tests
affected browser tests
privacy/security static checks
scoped lint/format
complete diff review
```

Use one independent Luna implementation review because the slice is authentication-adjacent and provider-sensitive.

Use one material repair cycle unless the re-review exception in this amendment is triggered.

## 14.4 ID-H implementation completion

Before candidate freeze record:

```text
ID-H_IMPLEMENTATION = PASS
ID-H_PLAYGROUND_DELIVERY_GATE = PENDING
```

This is not contradictory.

The product implementation can be complete before the live provider proof runs.

---

# 15. MIGRATION DECISIONS

This R2 amendment resolves the release decision for both migrations.

## 15.1 Migration 0031

Production is currently recorded as schema 30.

If the target remains schema 30, v0.8.3 requires:

```text
0031_canonical_identity_foundation.sql
```

because accepted v0.8.3 canonical identity and Staff Directory behavior depends on its schema.

Rules:

- additive/expand-first;
- no fabricated identity backfill;
- no destructive drop;
- backup/recovery before provider application;
- Playground before Production;
- schema/integrity/FK reconciliation after application.

## 15.2 Migration 0032

If the accepted final ID-G implementation contains:

```text
0032_staff_account_activity_history.sql
```

then the final v0.8.3 release requires it after 0031.

Rules:

- exact order `0031 -> 0032`;
- no fabricated historical backfill;
- append-only accepted history retained on forward-fix rollback;
- provider application only after local/Miniflare proof;
- exact schema/version reconciliation.

## 15.3 No shared/provider application during implementation

The migration decision is resolved.

Migration **execution** remains stage-gated.

Do not apply either migration to shared/provider Playground or Production D1 until the corresponding release stage.

---

# 16. PRE-FREEZE RELEASE GATE

The exact candidate may be frozen when all **provider-free product and deterministic release prerequisites** are green.

Required:

```text
ID-A = VERIFIED_NO_OP
ID-B = PASS
ID-C = PASS
ID-D = PASS
ID-E = VERIFIED_NO_OP
ID-F = PASS
ID-G = PASS
ID-H_IMPLEMENTATION = PASS

PASSWORD_VISIBILITY = PASS
PASSWORD_BROWSER_EVIDENCE = PASS

OPEN_P0 = 0
OPEN_P1 = 0

MIGRATION_0031_DECISION = REQUIRED_IF_SCHEMA30
MIGRATION_0032_DECISION = REQUIRED_IF_PRESENT_IN_ACCEPTED_ID_G

LOCAL_UPSTREAM_PARITY = PASS
WORKTREE = CLEAN
FINAL_REQUIRED_CHECKS = GREEN
PRODUCTION_STARTING_IDENTITY = VERIFIED
PLAYGROUND_ISOLATION_PREFLIGHT = PASS
PLAYGROUND_PROVIDER_CONFIG_PRESENCE = VERIFIED_WITHOUT_EXPOSING_VALUES
ROLLBACK_PLAN = VERIFIED
```

The following are **not pre-freeze requirements** because they depend on the frozen candidate/runtime:

```text
SOURCE_RECONCILIATION_PLAYGROUND_GATE
ID-H_PLAYGROUND_DELIVERY_GATE
PLAYGROUND_MIGRATION_ACCEPTANCE
PLAYGROUND_LIVE_BROWSER_ACCEPTANCE
```

---

# 17. FREEZE EXACT V0.8.3 CANDIDATE

Run one final appropriate release gate after ID-G and provider-free ID-H are integrated.

Then freeze and record:

```text
V83_FINAL_CANDIDATE_SHA:
V83_FINAL_CANDIDATE_TREE:
PACKAGE_VERSION:
LOCKFILE_HASH:
APPLICATION_ARTIFACT_HASH:
WORKER_IDENTITY:
SCHEMA_TARGET:
MIGRATION_ORDER:
PRODUCTION_START_SHA:
ROLLBACK_TARGET:
```

After freeze:

- no source change;
- no dependency change;
- no build-config change;
- no release-config change affecting the artifact/runtime.

Any such change invalidates the candidate.

Private Playground secret/binding activation needed to execute the already-tested runtime contract does not change source identity, but its exact external-state effect must be recorded and verified.

---

# 18. ISOLATED PLAYGROUND RELEASE

Deploy only the exact frozen candidate.

Use isolated Playground D1/R2/secrets/bindings.

Never point the Playground at mutable Production resources.

Required order:

```text
verify isolated Playground identity/bindings
-> capture Playground backup/recovery state
-> apply migration 0031 if required
-> reconcile 0031
-> apply migration 0032 if required
-> reconcile 0032
-> deploy exact frozen application/Worker candidate
-> automated acceptance
-> SOURCE_RECONCILIATION_PLAYGROUND_GATE
-> ID-H_PLAYGROUND_DELIVERY_GATE
-> focused authenticated/browser acceptance
-> final Playground reconciliation
```

If the repository's accepted deployment mechanism requires application deployment before migration rather than after it, follow that exact accepted runbook while preserving compatibility and rollback. Do not invent a sequence that contradicts the actual release tooling.

---

# 19. SOURCE RECONCILIATION PLAYGROUND GATE

The existing source-projection probe remains unchanged in code.

This amendment authorizes **one candidate-bound read-only live probe** when all are proven:

```text
ENVIRONMENT = STAGING
PLAYGROUND_MODE = true
IDENTITY_SOURCE_PROJECTION_PROBE_ENABLED = true

IDENTITY_SOURCE_PROJECTION_PROBE_AUTHORIZED_SHA
  = exact lowercase 40-character frozen candidate SHA

CANDIDATE_SHA
  = same exact SHA

actor has effective SYSTEM_ADMIN
service actor is SYSTEM_OWNER

Playground bindings are isolated
approved Google roster config is present
Google OAuth scope = spreadsheets.readonly
Production crossover = 0
```

The operation may:

- read the exact configured approved roster range;
- compare safe fingerprint/count summaries;
- return the existing allowlisted status.

It may not:

- mutate Google;
- mutate D1;
- apply/backfill canonical identities;
- auto-link people/accounts;
- infer privilege;
- expose raw rows;
- expose names/student IDs/emails;
- retry automatically.

## Pass

```text
COUNTS_MATCH_READ_ONLY
AND quarantineCount = 0
```

Then:

```text
SOURCE_RECONCILIATION_PLAYGROUND_GATE = PASS
```

## Failure

On:

```text
BLOCKED_NO_APPLIED_PROJECTION
BLOCKED_PERSISTED_PROJECTION_MISMATCH
BLOCKED_ID_C_RECONCILIATION_MISMATCH
BLOCKED_LIVE_SOURCE_MISMATCH
BLOCKED_QUARANTINE
SOURCE_UNAVAILABLE
TIMEOUT
```

record:

```text
PRODUCTION_ELIGIBLE = FALSE
```

Do **not** claim candidate freeze failed retroactively.

The candidate remains frozen unless source/config/code changes invalidate it.

Resolve only the exact discrepancy required by accepted authority.

No automatic roster mutation or forced count reconciliation.

---

# 20. ID-H PLAYGROUND DELIVERY GATE

After provider-free ID-H implementation passes and the exact candidate is deployed to Playground, perform the smallest safe live email-delivery proof required by accepted v0.8.3 scope.

Requirements:

```text
approved Playground environment
approved safe recipient/allowlist
no Production recipient crossover
no secret/recipient plaintext in Git/logs
exact candidate SHA
exact provider configuration
```

Prove at minimum, where the real provider path allows safely:

```text
8-digit delivery is generated/sent through the accepted provider
only current code is accepted
resend invalidates previous code
expiry is enforced
wrong/expired/reused code fails closed
provider failure does not mark verification successful
rate-limit/backoff remains active
```

Do not expose the actual OTP in ordinary logs/handoffs.

When green:

```text
ID-H_PLAYGROUND_DELIVERY_GATE = PASS
ID-H = PASS_FOR_RELEASE
```

---

# 21. PLAYGROUND ACCEPTANCE GATE

Production eligibility requires:

```text
FROZEN_CANDIDATE = EXACT
PLAYGROUND_MIGRATION_0031 = PASS / NOT_REQUIRED
PLAYGROUND_MIGRATION_0032 = PASS / NOT_REQUIRED
PLAYGROUND_AUTOMATED_ACCEPTANCE = PASS
SOURCE_RECONCILIATION_PLAYGROUND_GATE = PASS
ID-H_PLAYGROUND_DELIVERY_GATE = PASS
PLAYGROUND_AUTHENTICATED_BROWSER = PASS
PLAYGROUND_SCHEMA_RECONCILIATION = PASS
OPEN_P0 = 0
OPEN_P1 = 0
```

If exact accepted scope proves one external gate genuinely `NOT_REQUIRED`, Sol must record the exact accepted-spec reason. Do not use `NOT_REQUIRED` merely to accelerate the release.

---

# 22. SINGLE ROUTINE OWNER PAUSE

Do not ask Earl routine implementation questions before the Playground is ready unless a true material blocker requires an owner decision.

When all Playground gates are green, present one compact packet:

```text
V0.8.3 PRODUCTION GO REQUEST

candidate SHA:
candidate tree:
artifact:
Playground URL:
ID-A through ID-H:
migrations:
source reconciliation:
8-digit delivery:
automated acceptance:
browser acceptance:
open P0/P1:
Production current version/SHA:
Production current schema:
backup/rollback plan:
expected Production changes:
```

Wait for Earl's explicit:

```text
PRODUCTION GO
```

A green Playground is not Production authorization.

Any candidate-affecting source/config/artifact change after manual testing invalidates the GO.

---

# 23. PRODUCTION PREFLIGHT

After exact Production GO:

```text
fetch/reverify protected main
verify exact approved candidate
verify candidate/main tree-artifact parity method
verify Production bindings
verify current schema/migration
capture fresh backup/recovery evidence
verify rollback/forward-fix
verify secrets/config without printing values
verify no Playground crossover
verify required protected checks
```

Stop on any mismatch.

---

# 24. PRODUCTION RELEASE

Use the repository's protected accepted release mechanism.

Required outcome:

```text
protected accepted main lineage
-> exact tested tree/artifact parity
-> Production backup
-> migration 0031 if required
-> migration 0032 if required
-> exact accepted v0.8.3 deploy
```

No Playground data synchronizes back into Production.

Do not silently rebuild different source.

---

# 25. PRODUCTION SMOKE AND RECONCILIATION

Verify proportionately:

```text
readiness/version/release identity
public landing/request/lending surfaces
known authenticated account
Staff Directory authorization/read path
Activity History authorization/read path
8-digit verification path under accepted safe controls
schema version
latest migration
identity constraints
no inferred privilege
no private-data leak
no unexpected account/person/assignment mutation
audit/history append-only behavior
```

Post-deploy reconciliation must pass before recovery pointers move.

On P0/P1, security, privacy, migration, data-integrity, or rollback failure:

```text
STOP
PRESERVE EVIDENCE
EXECUTE ACCEPTED ROLLBACK / FORWARD-FIX
```

---

# 26. RECOVERY POINTER ROTATION

Rotate only after:

```text
PRODUCTION_DEPLOY = PASS
PRODUCTION_SMOKE = PASS
POST_DEPLOY_RECONCILIATION = PASS
ROLLBACK_READINESS = PASS
```

Use:

```text
old regression/r2         -> regression/r3
old regression/r1         -> regression/r2
old backup/last-known-good -> regression/r1
previous accepted main    -> backup/last-known-good
new accepted v0.8.3       -> main
```

Never rotate merely because a PR merged.

---

# 27. V0.8.3 RELEASE HYGIENE ONLY

After accepted Production closure:

- recompare temporary v0.8.3 branches;
- delete only zero-unique-work temporary branches;
- delete the release branch only after release proof;
- remove v0.8.3 task worktrees only after clean/unique-work proof;
- preserve unknown/historical/evidence worktrees;
- release the writer lock;
- leave broad repository/governance cleanup to v0.8.5.

Do not synchronize Context Vault AGENTS/project-policy replicas as part of this release.

That remains a separate gated governance task.

---

# 28. MINIMUM CONTEXT RULE FOR THE REMAINDER

Do not repeatedly reload the entire v0.8.3 history.

Use:

```text
checked-in AGENTS.md
CURRENT
CURRENT_TASK
CURRENT_HANDOFF
accepted v0.8.3 specification
this amendment
active slice plan
direct source/tests
verification failure evidence
exact release runbook when entering release stage
```

Expand only for:

- direct dependency;
- acceptance criterion;
- verification failure;
- migration/security/privacy risk;
- provider/release contradiction.

Do not turn `.codex/CURRENT.md` into a growing historical transcript.

Keep the next exact action clear.

---

# 29. WRITER / REVIEWER LIMIT FOR THIS AMENDMENT

For v0.8.3 completion:

```text
ORCHESTRATOR = GPT-5.6 Sol
SOL_WRITES = FORBIDDEN

CANONICAL_WRITER = ONE Terra MAX integration writer

ACTIVE_CHILD_MAX = 1

LUNA = READ_ONLY
```

The current Terra writer remains the canonical writer unless a verified transfer is required.

No second writer may touch overlapping ID-G/ID-H/current-chain paths.

No agent-spawning loops.

---

# 30. DO NOT REPEAT

Reuse fresh unchanged evidence for:

```text
ID-A auth/session preservation
canonical identity domain
email/provenance/linkage
active-access preservation
no-inferred-privilege authorization
two-stage approval
Staff Directory
password visibility
password browser evidence
ID-C provider-free reconciliation/probe implementation
disabled probe enablement predicate
release version correction
provider-free preflight facts whose state has not changed
```

Historical Activity History plan audits are complete.

Do not restart them.

---

# 31. FINAL SCOPE MATRIX BEFORE FREEZE

Before candidate freeze, the product matrix must read:

```text
ID-A = VERIFIED_NO_OP
ID-B = PASS
ID-C = PASS
ID-D = PASS
ID-E = VERIFIED_NO_OP
ID-F = PASS
ID-G = PASS
ID-H_IMPLEMENTATION = PASS

PASSWORD_VISIBILITY = PASS
PASSWORD_BROWSER_EVIDENCE = PASS
```

External release gates may still be:

```text
SOURCE_RECONCILIATION_PLAYGROUND_GATE = PENDING
ID-H_PLAYGROUND_DELIVERY_GATE = PENDING
```

because those require the exact frozen Playground candidate.

After Playground acceptance, final release matrix becomes:

```text
ID-A = VERIFIED_NO_OP
ID-B = PASS
ID-C = PASS
ID-D = PASS
ID-E = VERIFIED_NO_OP
ID-F = PASS
ID-G = PASS
ID-H = PASS_FOR_RELEASE

SOURCE_RECONCILIATION_PLAYGROUND_GATE = PASS
ID-H_PLAYGROUND_DELIVERY_GATE = PASS
PLAYGROUND_ACCEPTANCE = PASS
```

---

# 32. S17 100% DEFINITION

v0.8.3 is 100% only when:

```text
RELEASE = v0.8.3
STATUS = CLOSED
PRODUCTION = v0.8.3

ID-A THROUGH ID-H = COMPLETE

PLAYGROUND = PASS
EARL_PRODUCTION_GO = CONSUMED_FOR_EXACT_CANDIDATE
PRODUCTION_SMOKE = PASS
PRODUCTION_RECONCILIATION = PASS
ROLLBACK_READINESS = PASS

RECOVERY_POINTERS = ROTATED
TEMP_RELEASE_STATE = CLEANED

ACTIVE_WRITER = NONE
WRITER_LOCK = RELEASED
HANDOFF_STATUS = READY_FOR_HANDOFF
```

Write durable S17 evidence.

---

# 33. NEXT VERSION BOUNDARY

After v0.8.3 S17:

```text
NEXT_RELEASE = v0.8.4
NEXT_SESSION_REQUIRED = TRUE
NEXT_IMPLEMENTATION_STARTED = FALSE
```

Prepare a concise v0.8.4 handoff.

Then stop.

Do not begin v0.8.4 in this Codex session.

---

# 34. STOP CONDITIONS

Stop only for a material blocker:

- conflicting active writer;
- unknown work outside authorized paths;
- accepted-spec conflict not resolved by this amendment;
- P0/P1;
- private-data/secret exposure;
- authorization/security regression;
- migration integrity/FK failure;
- source reconciliation mismatch/quarantine at the Playground gate;
- provider/environment crossover;
- missing required private configuration at the exact external gate;
- missing rollback/forward-fix;
- exact candidate identity cannot be proven;
- Playground acceptance failure requiring owner/external truth;
- missing Earl Production GO;
- Production smoke/reconciliation failure.

Do **not** stop for:

- the superseded `ID_D=0` bookkeeping ambiguity;
- historical Activity History plan rejections already repaired;
- a P3 advisory with accepted residual disposition;
- a desire for another routine review after required green review;
- unrelated warnings;
- stale historical docs outside current acceptance;
- v0.8.4/v0.8.5 work.

---

# 35. SUCCESS CONDITIONS

```text
[ ] Fresh Context Vault authority was reconciled.
[ ] Active HAU project-policy sync gate was not bypassed.
[ ] Existing Activity History implementation was preserved and adopted.
[ ] No Activity History restart occurred.
[ ] All active Activity History paths remained authorized.
[ ] ID-B–ID-H owner mapping was made explicit.
[ ] ID-D was correctly reconciled to active-access preservation.
[ ] ID-G passed and integrated.
[ ] ID-H provider-free implementation passed.
[ ] No repeated plan-audit loop occurred.
[ ] Exact candidate froze only after provider-free product scope was green.
[ ] 0031 migration decision was applied correctly.
[ ] 0032 migration decision was applied correctly.
[ ] Isolated Playground passed migrations and automated acceptance.
[ ] Candidate-bound source reconciliation passed.
[ ] Live ID-H delivery proof passed.
[ ] Earl manually tested the exact candidate.
[ ] Earl gave explicit Production GO.
[ ] Production backup/rollback evidence passed.
[ ] Exact accepted v0.8.3 deployed.
[ ] Production smoke/reconciliation passed.
[ ] Recovery pointers rotated.
[ ] Safe v0.8.3 temporary state cleaned.
[ ] S17 closed.
[ ] ACTIVE_WRITER = NONE.
[ ] WRITER_LOCK = RELEASED.
[ ] v0.8.4 handoff prepared.
[ ] v0.8.4 implementation not started.
```

---

# 36. FINAL EXECUTION DIRECTIVE

**Finish v0.8.3 now.**

Continue the existing Activity History implementation from its current authorized isolated worktree.

Do not restart planning.

Do not repeat already-passed plan audits.

Finish ID-G, verify it proportionately, run the one required independent implementation audit, repair only material findings, and integrate.

Then execute ID-H as one coherent provider-free verification-lifecycle slice. Reuse existing correct code and repair only proven gaps.

When ID-A through ID-G plus `ID-H_IMPLEMENTATION` are green, freeze the exact candidate.

Then use that exact candidate in the Isolated Staging Playground for migrations, automated acceptance, the candidate-bound read-only source-reconciliation probe, live email-delivery proof, and focused browser acceptance.

Only after every Playground gate passes, pause for Earl's manual test and explicit Production GO.

After GO, complete protected main integration, Production migration/deploy, smoke, reconciliation, recovery-pointer rotation, release hygiene, and S17.

Release the writer lock, prepare the fresh-session v0.8.4 handoff, and **STOP**.
