# V0.8.1 V5 Owner Visual Feedback Amendment

Status: ACCEPTED - ACTIVE
Owner: Earl
Accepted: 2026-08-10, Asia/Manila
Required model: CODEX
Base specification: `.codex/specs/active/v0.8.1-v5-backend-integration-steer.md`
Branch: `release/v0.8.1-isolated-staging-playground`
Starting candidate SHA: `dfd1c78dbad789869a0cceae437144282470d2dd`
Released production baseline: `v0.8.0` at `3059098ff2a2935fec59df52748ccae420aadba7`
Schema: 30
Latest migration: `0030_production_access_and_operations.sql`
New migration: none authorized

## Supersession and candidate invalidation

Earl's 17 annotated browser comments are accepted manual-test feedback and
supersede the base specification only where they explicitly change the V5
landing, public sign-in, email-verification, public branding, governed design
media, or playground test-access behavior. The frozen prototype remains the
interaction and component authority everywhere else.

The previously frozen candidate is invalidated by this amendment. A new exact
candidate, tree, deterministic artifact, playground deployment, and Earl
approval are required.

## Authorized implementation

### Public landing

- Enlarge the public masthead and make its composition responsive and dynamic.
- Show USC and DOL marks together in the masthead.
- Change the masthead wordmark to `Holy Angel University Student Council`.
- Make the landing hero a large full-bleed/full-site-width image treatment.
- Use an official USC cover image from an owner-authorized/public official USC
  source when it can be obtained and preserved safely.
- Show only the USC mark in the hero overlay; do not show the DOL mark there.
- Change the hero primary action to `Staff sign in` and route it to the real
  staff sign-in surface.
- Correct the Request Center action color within the locked USC oxblood/gold/
  cream palette.
- Enlarge the official updates/public-announcement feature.
- Change the footer identity to `HAU-USC` and academic year `2026-2027`.

### Governed dynamic landing and design media

- Reuse and minimally extend the current brand/advertisement/media contracts so
  authorized operators can select, upload, preview, publish, schedule, roll
  back, and archive landing/design images where an image slot is part of the
  current V5 theme or design.
- The public landing may change its hero image, event title/copy, palette
  variant, official update, and supporting image according to the currently
  published major USC event configuration.
- Published server data is authoritative. Browser input must never select R2
  bindings, bucket names, object keys outside the accepted contract, or an
  unapproved arbitrary style payload.
- All media writes in this task target only the Isolated Staging Playground.
  Production media remains unchanged.
- If this requires a migration, new storage architecture, or unsafe arbitrary
  HTML/CSS, stop for a further amendment.

### Staff sign-in and verification

- Restyle the V5 staff sign-in page to match the current Production login page's
  proven form hierarchy, content sequence, privacy cues, errors, focus states,
  and responsive behavior while retaining the V5 design system.
- Verification start and resend must generate exactly eight numeric digits.
- Confirmation accepts exactly eight numeric digits and rejects all other
  shapes. Preserve non-enumeration, expiry, rate limits, one-time use, digest
  storage, and audit behavior.
- Tests may verify delivery payloads without a live provider/email send.

### Playground-only unlocked testing

- In the Isolated Staging Playground only, provide a server-authoritative
  full-capability test persona/session so Earl can open every registered test
  route without manually signing in.
- The shortcut must fail closed unless both `ENVIRONMENT=STAGING` and the
  server-owned playground mode are verified.
- Browser-supplied environment flags, role names, capabilities, account IDs, or
  resource identifiers cannot activate or expand the shortcut.
- Preserve a clear `Test real login flow` action that disables the shortcut for
  that test session and exercises normal Production-equivalent authentication.
- Production builds and runtime must contain no effective test-session bypass.

## Design authority

The existing V5 tokens, typography, motion, component grammar, responsive
behavior, and the supplied prototype remain locked. This amendment authorizes
only the explicit owner changes above. Hallmark may be used as a review and
quality discipline but must not introduce a competing theme or route structure.

## Verification

- Landing/header/hero/updates/footer visual checks at 320, 375, 390, 414, 768,
  1024, and 1440 CSS pixels, light and dark where supported.
- Exact USC/DOL placement, text, CTA destination, palette, and academic year.
- Published-event configuration and safe governed-media projection tests.
- Image upload/type/size/authorization/working-R2/production-denial tests.
- Eight-digit start, resend, confirmation, invalid-shape, expiry, rate-limit,
  replay, and non-enumeration tests.
- Playground unlocked route matrix plus real-login-mode and Production-denial
  tests.
- Current Production identity/readiness before and after remains v0.8.0 at the
  accepted SHA.
- Full repository, deterministic artifact, browser, visual, governance,
  handoff, Cloudflare dry-run, exact candidate, and playground acceptance gates.

## External action boundary

Authorized after local gates: bounded branch/PR writes, public read-only asset
retrieval from an official USC source, playground-only Worker deployment, and
playground-only governed brand/media writes required to verify this amendment.

Not authorized: Production deployment or mutation, Production R2/D1 write,
Google write, live provider/email send, migration, automatic promotion, merge,
tag, recovery-pointer rotation, M1/M2, force push, or unknown resource/branch
deletion.

## Stop condition

Stop after the new exact candidate is deployed and verified in the Isolated
Staging Playground. Earl must test it and issue a new explicit Production GO.

## Accepted Impeccable bolder refinement

Accepted: 2026-08-10, Asia/Manila
Owner instruction: `$impeccable bolder`

The exact candidate at
`9f66022916d9eb9ae440ec825cf6d51d3879feac` is invalidated for visual
acceptance. Amplify only the public landing's institutional masthead and
photographic hero so the first viewport feels more decisive and assured.

Authorized changes:

- Increase the masthead's scale, logo presence, wordmark authority, and
  full-width composition using only existing V5 tokens and motifs.
- Give the hero a stronger photographic fold, clearer asymmetric hierarchy,
  more concentrated oxblood contrast, and more decisive action treatment.
- Preserve the existing governed event image, published copy, CTA destinations,
  USC-only hero mark, USC/DOL masthead marks, themes, responsive behavior, and
  reduced-motion contract.

Everything else stays unchanged: landing intro, portal rail, updates, footer,
public authentication surfaces, authenticated workbench, route registry,
backend contracts, data, schema, and environment guards.

Verification must include one batched desktop/mobile light/dark inspection,
320 through 1920 CSS-pixel overflow and first-fold checks, the Impeccable
mechanical detector exactly once after the UI is complete, deterministic
artifact verification, Production-marker denial, and exact playground-only
deployment. No Production, D1/R2 business-data, Google, or provider-email write
is authorized.
