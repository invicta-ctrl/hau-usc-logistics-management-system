# v0.7.0 Phase 23 Accessibility, Performance, and Capacity Handoff

Status: ACCEPTED ON STAGING — PRODUCTION UNTOUCHED

Accepted staging runtime: `d095685e223be2697cc72582d35967e70cfd5163`

Schema: 29 / `0029_reusable_asset_reassignment.sql`

## Accepted scope

Phase 23 completed responsive accessibility, performance measurement, and
bounded non-abusive capacity acceptance against the exact staging runtime at
390 px, approximately 820 px, and 1366 px. Production Worker, D1, R2,
configuration, routes, secrets, data, tags, and releases were not modified.

## Repairs made during acceptance

- Shared buttons now retain a 24 px minimum target, policy actions retain a
  44 px target, and announcement indicators preserve their 12 px visual form
  inside a 24 px hit area.
- The small gold eyebrow text now meets WCAG AA contrast on the cream surface.
- Google roster placeholder validation now rejects only complete placeholder
  values; random private-key base64 containing an incidental `TBD` or `TODO`
  substring can no longer make CI nondeterministic.
- Public request and lending limiter behavior has explicit unit coverage for
  the ten accepted attempts followed by truthful HTTP 429 denial.

## Responsive and accessibility proof

- Live Playwright acceptance passed 5 / 5 on exact runtime `d095685`.
- Public login, requester, and lending paths and protected System Owner paths
  passed at 390 px, approximately 820 px, and 1366 px without horizontal
  overflow, clipped required actions, or hidden controls.
- Keyboard operation, visible focus, labels, validation errors, screen-reader
  names, representative contrast, touch targets, reduced motion, and desktop
  200% zoom passed.
- Cache-busted navigation and no-cache browser contexts prove the accepted
  static assets belong to the exact runtime rather than an earlier edge copy.

## Performance and capacity proof

- Twenty-one viewport measurements recorded page completion at
  912.9 / 1,620.5 / 2,399.4 ms minimum/median/maximum; DOM interactive at
  105.2 / 326.7 / 578.4 ms; and DOM complete at
  378.9 / 1,091.9 / 1,860.6 ms.
- Ten repeated health/catalog API measurements recorded
  89.6 / 103.8 / 385.4 ms and payloads of 441 / 531.5 / 622 bytes
  minimum/median/maximum. Fifteen private remote D1 measurements recorded
  0.27 / 0.61 / 1.59 ms.
- Login bootstrap and protected route transitions were measured at all
  required widths. The REST runtime remained action-driven: no scoped-revision
  polling request occurred during a 16.5 second observation window.
- Bounded live login, public requester-denial, and lending bursts failed
  closed at their accepted limits without abusive load.
- Focused auth, public-policy, composite-request, circulation, concurrency,
  and idempotency coverage passed 52 / 52. Exact-lineage Phase 22 deployed
  proof for concurrent reservation exclusion and idempotent retries remains
  valid because those runtime paths were unchanged.

## Repository and CI verification

- `npm run check`: pass; governance, lint, 74 Vitest files / 480 tests,
  deterministic build and packaging, Apps Script parity, distribution checks,
  Cloudflare types, and deployment dry-run all passed.
- PR #9 exact product-head checks passed 6 / 6: validate, verify, build,
  browser-smoke, deploy, and report-build-status.
- Cache-busted health reports STAGING, exact runtime `d095685`, schema 29,
  migration 0029, connected dependencies, and ready runtime state.

## Reconciliation and immutable evidence

- The temporary System Owner is disabled, has zero sessions, and its private
  credential returns HTTP 403. One enable audit and one disable audit remain.
- A shell-quoting defect made those two immutable audit payloads non-JSON.
  They were not altered or deleted; one append-only valid correction audit
  records the exact final disabled state and corrected action meaning.
- Nineteen Phase 23 limiter rows were removed exactly. Eight older auth rows
  aged out through the repository's normal sliding-window pruning; no unrelated
  rows appeared. The two rows generated by final disabled-credential denial
  were then removed, leaving zero Phase 23 limiter rows.
- Private browser, timing, D1, actor, cleanup, and audit evidence remains
  outside Git. Sensitive identifiers, credentials, provider values, URLs, and
  row-level data are intentionally omitted here.

## Boundary and next phase

Phase 23 passes. Continue directly to Phase 24 backup, cleanup, retention, and
the required real staging rollback rehearsal. Production remains NO-GO.
