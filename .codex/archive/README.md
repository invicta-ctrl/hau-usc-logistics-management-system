# Archived Codex Records

These files are immutable historical specifications, handoffs, and release evidence. They are not current authority. Cold starts use `AGENTS.md` and the canonical `.codex/CURRENT*` chain; historical records are read only when the active task explicitly requires them.
