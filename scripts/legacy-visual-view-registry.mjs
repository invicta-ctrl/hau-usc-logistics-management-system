// Stable view order for the retained Apps Script recovery sidecar. This is not
// an application build registry and is intentionally isolated from the React app.
export const legacyVisualViewIds = Object.freeze([
  'overview',
  'request',
  'lending',
  'release',
  'restocking',
  'procurement',
  'inventory',
]);
